name: Test
on:
  push:
    branches:
      - "main"

jobs:
  release:
    name: Test
    runs-on: macos-10.15
    steps:
      - name: Check Clang Version
        run: |
          clang --version
      - name: Install llvm
        run: |
          brew reinstall llvm
      - name: Check Clang Version
        run: |
          clang --version

